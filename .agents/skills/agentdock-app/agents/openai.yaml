interface:
  display_name: "Agentdock App"
  short_description: "Repo-specific patterns and workflows for agentdock-app"
  default_prompt: "Use the agentdock-app repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true